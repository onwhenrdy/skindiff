#ifndef SC_MACROS_H
#define SC_MACROS_H

#define STRING2(x) #x
/**
  * @brief Macro ist used to transform preprocessor define values into strings
  * @param[in] VARIABLE which value should be transformed into a string
  */
#define STRING(x) STRING2(x)


#endif // MACROS_H
