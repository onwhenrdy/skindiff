% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/units.R
\name{skin_units}
\alias{skin_units}
\alias{um}
\alias{mm}
\alias{cm}
\alias{cm2}
\alias{mm2}
\alias{ml}
\alias{mg_per_ml}
\alias{ug_per_ml}
\alias{ng_per_ml}
\alias{um2_per_min}
\alias{cm2_per_s}
\alias{seconds}
\alias{minutes}
\alias{hours}
\alias{days}
\alias{mg_per_cm2}
\alias{ug_per_cm2}
\alias{ng_per_cm2}
\title{Unit helpers for skindiff}
\usage{
um(x)

mm(x)

cm(x)

cm2(x)

mm2(x)

ml(x)

mg_per_ml(x)

ug_per_ml(x)

ng_per_ml(x)

um2_per_min(x)

cm2_per_s(x)

seconds(x)

minutes(x)

hours(x)

days(x)

mg_per_cm2(x)

ug_per_cm2(x)

ng_per_cm2(x)
}
\arguments{
\item{x}{A numeric vector.}
}
\value{
A `units` object carrying the indicated unit.
}
\description{
Thin wrappers around [units::set_units()] that build values with the
right unit attached. Use these (or call `units::set_units()` directly)
anywhere a `skindiff` constructor expects a unit-bearing argument.
}
\details{
Every constructor in `skindiff` rejects bare numerics for unit-bearing
arguments -- that is the whole point of unit safety. If you have a value
in a non-canonical unit (e.g. millimetres for a layer thickness), pass
`mm(0.11)` and the package converts internally to micrometres.

Note that `minutes()` is preferred over `min()` to avoid masking
[base::min()].
}
\examples{
um(110)               # 110 [um]
mm(0.11)              # 0.11 [mm]   -- equivalent to um(110)
mg_per_ml(127.27)
hours(24)

}
