% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{vehicle}
\alias{vehicle}
\title{Build a vehicle (donor) compartment}
\usage{
vehicle(
  c_init,
  height,
  D,
  finite_dose = TRUE,
  replace_after = NULL,
  remove_at = NULL,
  name = "Vehicle",
  log_mass = TRUE,
  log_cdp = FALSE
)
}
\arguments{
\item{c_init}{Initial donor concentration. Must have units of
concentration (e.g. `mg_per_ml(127.27)`, `ng_per_ml(50)`).}

\item{height}{Donor thickness. Must have units of length (e.g.
`um(110)`, `mm(0.11)`). Rounded to integer micrometres internally.}

\item{D}{Diffusion coefficient inside the donor. Must have units of
area-per-time (e.g. `um2_per_min(9.27)`, `cm2_per_s(1e-7)`).}

\item{finite_dose}{If `FALSE`, the donor concentration is clamped at
`c_init` for the entire run (true Dirichlet boundary at the
donor/skin interface).}

\item{replace_after}{Donor refresh period (units of time, e.g.
`hours(24)`), or `NULL` to disable. When set, every donor cell is
reset to `c_init` at every multiple of this duration.}

\item{remove_at}{Donor removal time (units of time), or `NULL` to
disable. When set, the donor is deleted at this time and the
simulation continues on (skin layers + sink).}

\item{name}{Compartment label used in result output.}

\item{log_mass, log_cdp}{Whether to record the mass time-series and/or
the concentration-depth profile for this compartment.}
}
\value{
A `skin_vehicle` object (a classed list) ready for [skin_params()].
}
\description{
Describes the donor formulation that sits on top of the skin. Every
unit-bearing argument requires a `units` object built with
[units::set_units()] or one of the `skindiff` helpers (e.g. [um()],
[mg_per_ml()]). Bare numerics are rejected.
}
