% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{layer}
\alias{layer}
\title{Build a skin-layer compartment}
\usage{
layer(
  name,
  height,
  D,
  K,
  cross_section,
  c_init = mg_per_ml(0),
  log_mass = TRUE,
  log_cdp = FALSE
)
}
\arguments{
\item{name}{Layer label (e.g. `"Stratum corneum"`).}

\item{height}{Layer thickness. Must have units of length.}

\item{D}{Diffusion coefficient. Must have units of area-per-time.}

\item{K}{Partition coefficient relative to the vehicle (dimensionless,
strictly positive). At equilibrium, `c_layer = K * c_vehicle`.}

\item{cross_section}{Fraction of the application area occupied by this
layer (dimensionless, in `(0, 1]`).}

\item{c_init}{Initial concentration in this layer. Must have units of
concentration. Defaults to `mg_per_ml(0)`. Almost always 0.}

\item{log_mass, log_cdp}{Whether to record the mass time-series and/or
the concentration-depth profile for this compartment.}
}
\value{
A `skin_layer` object (a classed list) ready for [skin_params()].
}
\description{
Layers are passed top-to-bottom (closest to vehicle first). The layer's
physical area is `area * cross_section`, where `area` is set on
[skin_params()].
}
